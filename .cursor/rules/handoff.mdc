---
description: 每切片必须更新 HANDOFF.md，对齐代码状态、交接与验收
alwaysApply: true
---

# HANDOFF 切片约定

每个可交付工作切片结束时（功能可测、或明确暂停点），必须更新仓库根目录 [`HANDOFF.md`](../../HANDOFF.md)，再结束本轮或提 commit。

## 三者对齐

| 轴 | 要求 |
|----|------|
| 代码状态 | 「正在进行 / 已完成」与分支、`git status`、关键路径一致 |
| 交接文档 | 按 HANDOFF 固定章节写清；下一步必须是一条可立即执行的动作 |
| 验收标准 | 写明行为、测试命令、成功条件；未测项放在「已知问题」 |

## 必须维护的章节

保持并填写：`# 当前目标` · `# 已完成` · `# 正在进行` · `# 技术决策` · `# 已知问题` · `# 验收标准` · `# 下一步`。

「已完成」尽量带 commit hash 与主要文件；「正在进行」精确到文件 / 模块，并标明是否有未提交修改。

## 反例

- 只改代码不改 HANDOFF
- 下一步写成空泛的「继续优化」
- 验收标准与当前目标无关，或命令已失效却不改

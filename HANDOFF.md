# HANDOFF

> 每完成一个可交付切片更新本文件，使代码状态、交接与验收一致。约定见 [.cursor/rules/handoff.mdc](.cursor/rules/handoff.mdc)。

**最后更新：** 2026-09-14

**分支：** `main`；近景演出、背景音乐、相关测试及设计文档收录于 `a1d3985`，已推送并部署。本文与 README、DEPLOY 的后续提交只记录发布结果，不改变游戏构建。

**当前切片：** 近景与音乐增强已上线。北京时间 2026-09-14 08:50 验证线上 14/14 个公开文件与 `a1d3985` 构建完全一致。

**发布任务（2026-09-14）：** 用户要求「提交，并部署」已完成。通过现有 Git HTTPS 凭据推送，触发 [GitHub Actions 34793818430](https://github.com/shadowkael/roll/actions/runs/34793818430)，测试构建、上传及 Nginx 重载全部成功。

# 当前目标

完成《那个谁》首个可从入学玩到章末回忆的军训章节，让用户实际检验自由探索、校园时间、选择后果，以及人物因理解而逐渐清晰的体验。

本轮响应用户对「场景镜头太远、纯文字缺少沉浸感」的反馈：保留已认可的大地图探索，将细节场景转换为环境、人物与手部物件三个景别；用动画和音乐承接文字。受众为十七至十九岁的准大学生／大学生，以及毕业近二十年、接近四十岁的玩家，突出共同经历的日常细节。

设计对齐已经完成。用户明确回复「与我想要的体验一致，请开始进行具体设计实现」，授权按已讨论方向重建工程。主角留白、2D 斜俯视、可自由移动、比例时间／离线与阅读暂停、校历和条件事件、首版完整军训篇均已确定，不再作为待确认项。

# 已完成

- **近景演出：** 新增 `src/scene-content.mjs`、`src/cinematic.mjs`、`src/encounter.css`。全部 17 段剧情、50 个选择反应，以及日常、环境和休息入口均有分镜；6 个地点使用新环境图集。人物、肢体、手与道具实际绘制并动画，支持 20 种物件、12 种手势；玩家递还物件和踏步使用匿名的前景手脚。
- **阅读与观察：** 一镜一段文字，观察热点触发短动作并推进；可逐镜继续、直接回应或回看。热点只供观察，不提前替玩家作选择；原选项在末镜出现。角色的清晰变化约 1.25 秒渐显，菜单、隐藏页面冻结演出；减少动态保留即时交互。
- **背景音乐：** 新增 `src/audio.mjs`，原创八小节日间／夜间轻钢琴与拨弦，近景与图书馆减薄配器；练习停掉背景音乐，保留训练节拍。首次进入游戏后启用，音乐／音效独立开关，音乐音量可调并保存；旧静音偏好保留。
- **交互修复：** 练习期间「回到校园」先登记跳过再关闭，避免留下不可见练习状态；设置滑块和下拉框持有焦点时 Esc 仍可返回，焦点循环包含表单控件；聚焦按钮时空格只触发一次。
- **内容与模拟：** `src/content.mjs`、`src/simulation.mjs` 实现九月一日至五日，六个校园地点、四位 NPC、五次校历事件、十二段个人剧情，以及同日常相遇相结合的关系变化。缺席、迟到、取消选择、补领军训服和提前休息都有后续，完全错过主线也能结局。
- **探索与地图：** `src/world.mjs` 用 Canvas 绘制斜俯视校园，支持坐标投影、点击命中、镜头平移／缩放、建筑与池塘碰撞，以及绕障路径。`src/main.mjs` 接入键盘、鼠标与触控输入。
- **人物与美术：** `public/art/campus-cover.png`、`public/art/portraits.png` 与本轮 `public/art/scene-backgrounds.png` 已接入。探索人物、动画近景和记忆手册使用同一关系状态；四阶段清晰变化在过程中发生，姓名按实际介绍显示，主角不生成固定姓名、身世或最终脸孔。
- **界面与存档：** 扉页、继续／重新开始、日程、地点导航、记忆手册、设置、通知、自动本地存档和章末回忆均已接入。恢复存档不计算离线时间；选择前与反应阶段保留独立状态，避免重复登记经历。
- **训练互动：** 自荐领队进入八拍练习，支持空格或轻触、主动跳过；成绩与跳过各有回应和真实记忆，不设置通关门槛，也不把准确度转换成羁绊。
- **回忆导出：** 章末可下载本局实际经历的 UTF-8 文本，文件名为 `那个谁-九月的回忆.txt`。没有实际经历的分支不会被写进回忆。
- **工具与部署：** 原生 Node 开发服务器及构建脚本、测试入口、手动 SSH 部署与 CI 已统一。构建产物仅为 `dist/`；`.mjs` MIME 与 `no-cache` 策略一致；部署不递归删除远端目录。保留既有账号、私钥与环境变量配置。
- **发布与线上核验：** 本轮提交 `a1d3985` 已通过 CI 发布。14/14 个公开文件的 SHA-256 和字节数完全匹配，HTTP 状态、MIME、缓存及 `nosniff` 正确，缺失 `.mjs` 返回 404。此前对 `eb6d132` 的九文件检查保留为历史记录。
- **旧实现清理：** 删除旧 CSS/JS/Ink/story 运行工程、对齐工具、Ink/vendor/extract/hooks 安装脚本和两套过时初始化脚本。`githooks/pre-commit` 改为 `npm test`，不自动修改或暂存文件。保留 POC、原始 assets 和全部设计文档。
- **文档：** 更新 README、本文和 `docs/REBUILD_DESIGN.md`；新增 `docs/MILITARY_CONTENT.md` 与 `docs/ART_DIRECTION.md`；部署说明已经与新命令一致。

基础军训篇为 `eb6d132`，本轮增强为 `a1d3985`，均已在远端历史中；当前线上使用后者。旧 2026-07 的 Ink 迁移和 Scene1 图层工作可在 Git 历史中查阅，不再代表当前运行时结构。

# 正在进行

- 本地试玩入口为 `http://localhost:3000/`，开发服务仅监听本机。本轮直接在该站点验收了近景、回应恢复、回看、菜单键盘操作和跨日路径。
- 近景、声音、美术、测试与设计文档已收录于 `a1d3985`。本次后续仅更新发布结果文档，使用 `[skip ci]` 避免文档提交再次上传相同游戏文件；本地提交检查仍运行。
- 云端发布与 HTTP 核验已完成。内置浏览器公网导航超时；本地交互验收结果与线上文件一致性分别记录，不将其混同为线上浏览器交互回归。

# 技术决策

- **时间：** 标准现实 1 秒对应校园 2 分钟，设置可切换 2／4 倍。扉页、菜单、对话、节拍练习和页面隐藏时暂停。普通交谈结束不扣阅读时间；校历事件选择完成后，在回应页关闭时推进到活动结束。
- **跨日：** 第一天 15:20 开始，之后 07:00 开始；19:00 后可在宿舍休息。每日 22:00 停时，第五日 22:00 自动结局；结营后也可回宿舍提前进入回忆。
- **关系：** `clarity` 与 `trust` 分离。每个角色的三段个人经历至少跨三个游戏日；一次主线事件最多贡献初步留意。唯一经历 ID 防止重复点击或读档刷进度，分歧不降低已经得到的清晰度。
- **事件：** 日期、时段、地点和前置经历共同决定入口；集合期间同场人物优先进入当前校历活动，防止日常开场覆盖训练。
- **表现：** 校园是实际可行走的空间，封面不作为地图。近景由插画背景和可动 Canvas 人物构成；环境镜完整交代空间，人物镜推近半身，道具镜聚焦手与物件。面容从留白逐层出现，衣服、发型和动作从初见即可辨认；记忆手册继续使用肖像图集。
- **数据边界：** `content` 管故事，`simulation` 管游戏状态，`world` 管地图，`scene-content` 将真实剧情投射为镜头计划，`cinematic` 管演出，`audio` 管声音，`main` 管浏览器交互。分镜没有新增游戏状态或存档版本。开发与构建不依赖 Ink 或前端打包器，`ssh2` 仅用于部署。
- **存档：** localStorage 单浏览器单存档；开启新一局需要确认，旧版 Ink 存档不迁移。无账号、云同步、后端与多人状态。
- **发布：** 手动与 CI 使用相同 `dist` 布局，部署目录与 Nginx `root` 取同一 `DEPLOY_PATH`。固定资源名使用 `no-cache`，不配置 immutable 强缓存。现有 Git HTTPS 推送和 CI 服务器认证已实际验证可用。

# 已知问题

- 首版用五天压缩军训过程，不是完整大学四年的内容；考试、社团、后续学期与多章节关系延续尚未实现。
- NPC 根据时段直接切换活动点，没有连续走路或自主规划行为。近景为程序化二维肢体动画，尚无口型、配音、逐帧表情或真人表演捕捉。
- 音乐与声音均由 Web Audio 原创合成，没有实录乐器、完整长篇配乐或实录环境声。自动化验证了配乐、调度和开关，尚未完成不同耳机及手机扬声器的主观听感验收。
- 存档依赖当前浏览器的网站存储，清理数据或使用另一设备无法自动找回。存储受限时页面内仍可游玩，但无法保证关闭后恢复。
- 旧 `POC/` 与 `docs/INK.md`、`docs/ART_WORKFLOW.md` 等含已移除路径和旧产品设定，作为历史参考保留。当前行为以 README、重做设计、军训内容文档及代码为准。
- 本机直接 SSH 认证尚未恢复，`gh` 未登录；可通过现有 Git HTTPS 凭据推送并触发 CI。若需要手动运行 SSH 部署，再配置已授权密钥。本次首次推送连接中断，确认远端未更新后用命令级 HTTP/1.1 和 16 MiB 上传缓冲重试成功，没有修改全局 Git 配置。
- 手机布局已通过浏览器窄视口目视检查；尚未进行 iOS／Android 真机、真实触控及性能测试。

# 验收标准

## 已执行的分项验证

- `node --test tests/simulation.test.mjs`：27 项通过；覆盖完整章节、四人人物路线、阅读暂停、事件优先级、信任／清晰分离、选择前后读档和练习结果等。
- `node --test tests/world.test.mjs`：6 项通过；覆盖地图碰撞、端点、绕障和路径安全。
- `node --test tests/server.test.mjs`：6 项通过；覆盖静态资源 MIME、URL 解码、HEAD、缺失资源、路径穿越、构建过滤与部署目录校验。
- 开发服务器测试使用仅监听 `127.0.0.1` 的临时端口；受限沙箱需授予本机监听权限。没有为了通过测试跳过这些检查。

## 基础军训篇整合验证记录（`eb6d132`）

- `npm test`：39 项通过，0 失败（27 项模拟、6 项地图、6 项服务器与构建）。
- `npm run build`：成功生成 `dist/`；`node --check src/main.mjs` 与 `git diff --check` 通过。
- 浏览器实际从第一日玩至第五日回忆：初遇室友、错过领衣后补领、按日休息、沿路线去操场、站军姿、自荐领队、跳过练习、结营、回宿舍收好回忆。
- 刷新后恢复选择页、选择后的回应页与未完成的节拍练习；回应、关系和回忆没有重复登记。打开菜单后停留超过 30 秒，校园时钟保持不变；Esc 关闭菜单后练习仍存在。减少动态设置在刷新后保留。
- 目视检查桌面扉页／校园／对话，以及手机窄视口下的近景、选项、顶部按钮和校园缩放。未相识与初步留意的肖像遮罩可见；四人全部理解阶段及跨日路线由模拟测试覆盖。
- 章末回忆与实际选择一致，未相识角色保持未知。已点击文本导出入口，浏览器无报错；内置浏览器未返回下载文件路径，未做落盘文件内容复核。
- 完整游玩过程中浏览器 `error`／`warn` 日志为空。最后的宿舍入口文案调整由模拟测试覆盖。

## 线上文件与 HTTP 核验（2026-09-13）

- 站点：[http://139.224.30.109:8000/](http://139.224.30.109:8000/)。入口、四个 ES 模块、样式、两张 PNG 与 favicon 共 9 个文件，与本地 `eb6d132` 构建产物逐项 SHA-256 一致。
- 公开文件均返回 200；HTML、JavaScript、CSS、PNG、SVG 的 MIME 正确。响应包含 `Cache-Control: no-cache` 与 `X-Content-Type-Options: nosniff`。
- 不存在的 `.mjs` 返回 404，没有错误回退为 HTML。
- 这些历史结果来自 2026-09-13 上传前的只读核验；当时未进行上传、Nginx 修改或代码推送，也不能据此推断此前由谁执行了发布。

## 近景与声音增强本地验收（2026-09-14）

- `npm test`：140 项通过，0 失败。包含原有 39 项，以及 83 项分镜／选择反应测试、11 项音频测试、7 项近景渲染测试。`npm run build` 成功，`node --check src/main.mjs` 和 `git diff --check` 通过。
- 桌面浏览器：宿舍初遇的热点动作、末镜选项、选择后面容变化、回看全文、直接跳至回应、回应阶段刷新继续均可用。回应读档后手册只保留一次初遇记忆。
- 手机 390 × 844 视口：检查宿舍近景、道具特写、回看浮层、三选项领队场景和训练节拍布局。人物及手中物件可见，选项可点击，视口覆盖已恢复默认。尚未进行真机触控及帧率测试。
- 从初遇推进至第四日，跨日与缺席后的补领分支正常。领队测试另使用独立本机测试端口、由模拟模块生成的第四日存档，避免受浏览器自动化往返耗时影响；该临时入口不在项目或构建产物中。
- 领队回归：自荐后进入带动画背景的练习；菜单保持 `0 / 8` 不推进；练习中从近景返回校园，会记录「先看一遍」并结束活动至 11:00；刷新不重新进入已完成的练习。未完成练习刷新后能恢复，主动跳过后呈现对应的逐镜回应。
- 音乐与音效开关独立；关闭音乐、音量改为 36% 后刷新，偏好保持且音效仍开启。聚焦音量滑块时 Esc 正常关闭设置。音频实际配乐的主观听感尚未完成跨设备验收。
- 本轮页面浏览器 `error`／`warn` 日志为空。所有剧情与动作契约由自动化覆盖；本轮没有逐一浏览全部 50 个反应分支，也没有重新完整游玩第五日结局。

## 本轮提交与发布验收（2026-09-14）

- 游戏提交：`a1d3985a8003d514531d6ef43b6ca48eb1ddc629`；提交钩子 `npm test` 再次通过 140 项。`npm run build` 成功。
- 推送后 CI 运行 `34793818430` 的部署任务成功，包含测试构建、目的目录校验、完整构建上传和 Nginx 配置重载。
- 线上 14/14 文件与本地构建的 SHA-256、字节数一致，全部 HTTP 200、正确 MIME、`no-cache`、`nosniff`。五个新增文件均已上线；不存在的模块正确返回 404。
- 内置浏览器打开公网入口超时，未完成额外线上交互回归；没有为此改变服务器安全配置或站点地址。

## 验收边界

自动化覆盖所有主线与四人理解路线。基础版本已完成一条包含缺席和可选练习的整章浏览器路线；本轮增强另覆盖上述近景、读档、跨日和训练回归。最新线上核验对应 `a1d3985`，限于文件和 HTTP 层。测试没有穷举所有浏览器、输入设备、剧情演出的视觉效果或真实设备听感。

# 下一步

打开[线上试玩入口](http://139.224.30.109:8000/)，走近人物，依次体验观察热点、人物近景与选择后的动作；从设置试听背景音乐并调整音量。下一次游戏更新继续使用现有 Git 推送与 CI 发布路径。

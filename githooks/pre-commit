#!/bin/sh
# Validate the game without changing or staging the working tree.
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1

if [ ! -f package.json ]; then
  exit 0
fi

echo "==> pre-commit: npm test"
npm test
